import 'package:flutter/widgets.dart';

class WidgetSettings extends StatelessWidget {
  const WidgetSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Color.fromARGB(0, 6, 72, 255),
      child: Text('Index 3: Ajuste'),
    );
  }
}
